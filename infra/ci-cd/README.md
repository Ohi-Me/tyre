# infra/ci-cd

CI/CD configuration and helpers. GitHub Actions workflow YAMLs live in
\`.github/workflows/\` (repo root) per GitHub convention; this folder holds
shared CI scripts, image build helpers, and deployment manifests that don't
fit under \`kubernetes/\` or \`helm/\`.
